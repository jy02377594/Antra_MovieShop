@using ApplicationCore.ServiceInterfaces

@inject IGenreService _genreService


@foreach (var genre in await _genreService.GetAllGenreModelsAsync())
{
    @*@Html.ActionLink(genre.Name, "Genre", "Movies", new { genreId = genre.Id }, new { @class = "dropdown-item" })*@

    <a asp-action="genre" asp-controller="Movies" class="dropdown-item" asp-route-gid="@genre.Id"> @genre.Name</a>
}